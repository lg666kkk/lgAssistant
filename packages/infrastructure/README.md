# @repo/infrastructure

Production adapters for databases, caches, external providers, telemetry, and
other I/O. This package implements application ports; application code must not
import these adapters directly.
