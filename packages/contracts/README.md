# @repo/contracts

Shared transport contracts used by the browser and server adapters.

This package must stay independent from React, Next.js, databases, environment
variables, and Agent Runtime implementations. It may contain serializable DTOs,
event unions, request/response contracts, and pure serialization helpers.
